export default function Count() {
  return <p className="count">0</p>;
}



















/*export default function Count({ size, number }) {
  //const countName = props.number;
  //const {size, number} = props;
return (
    <>
    <CountTitle />
    <p>{number}</p>
    </>
);
}

function CountTitle() {
    return <h2>Current count is:</h2>;
}
*/
